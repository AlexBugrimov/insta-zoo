spring:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/insta-zoo?useSSL=false
    username: zoo
    password: password
server:
  port: 8081
